{
  "name": "bs-test-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "clean": "bsb -clean-world",
    "build": "lerna run build --stream",
    "watch": "lerna run watch --stream"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "bs-platform"
    ]
  }
}
